import { ShowMoreDirective } from './show-more.directive';

describe('ShowMoreDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowMoreDirective(null);
    expect(directive).toBeTruthy();
  });
});
