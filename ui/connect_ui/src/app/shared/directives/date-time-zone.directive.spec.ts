import { DateTimeZoneDirective } from './date-time-zone.directive';

describe('DateTimeZoneDirective', () => {
  it('should create an instance', () => {
    const directive = new DateTimeZoneDirective(null);
    expect(directive).toBeTruthy();
  });
});
