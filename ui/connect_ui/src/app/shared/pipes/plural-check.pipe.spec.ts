import { PluralCheckPipe } from './plural-check.pipe';

describe('PluralCheckPipe', () => {
  it('create an instance', () => {
    const pipe = new PluralCheckPipe();
    expect(pipe).toBeTruthy();
  });
});
