import { ValueAlterPipe } from './value-alter.pipe';

describe('ValueAlterPipe', () => {
  it('create an instance', () => {
    const pipe = new ValueAlterPipe();
    expect(pipe).toBeTruthy();
  });
});
